class Question {
  String questionText;
  bool questionAnswer;

  /* Question({String q,bool a})
  {              //constructor
    questionText= q;
    questionAnswer=a;
  }
}*/
  // another way
 // Question({this.questionText, this.questionAnswer});
  // third way
 Question(this.questionText, this.questionAnswer);

}